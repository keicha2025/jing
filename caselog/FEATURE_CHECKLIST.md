# Caselog 功能檢核清單

> 最後更新：2026-03-02T23:41:00+08:00

---

## 1. 專案管理

| # | 功能 | 狀態 | 備註 |
|---|------|------|------|
| 1 | 新增專案（名稱、預算、目標時薪） | ✅ | `AddProjectModal` |
| 2 | 編輯專案資訊（名稱、預算、時薪） | ✅ | `EditProjectModal` |
| 3 | 刪除專案（需確認） | ✅ | 選單底部操作 |
| 4 | 標記專案完成 / 重啟 | ✅ | 選單操作切換 |
| 5 | 專案期間設定（起迄日期） | ✅ | `startDate` / `endDate` |
| 6 | 專案列表頁面 | ✅ | `/projects` 路由 |
| 7 | 點擊卡片進入專案詳情 | ✅ | `ProjectCard` → `/project?id=` |

---

## 2. 任務管理

| # | 功能 | 狀態 | 備註 |
|---|------|------|------|
| 8 | 新增子任務（含初始工時 H/M 分開輸入） | ✅ | `AddTaskModal` |
| 9 | 勾選任務完成 / 取消完成 | ✅ | 點擊圈圈或右滑 |
| 10 | 滑動任務卡片顯示「編輯」按鈕 | ✅ | 左滑出現 `edit` 按鈕 |
| 11 | 點擊任務展開工時紀錄明細（摺疊式） | ✅ | `AnimatePresence` 動畫 |
| 12 | 工時明細時間格式 HH:MM | ✅ | `fmtMin()` 函數 |
| 13 | 工時明細顯示紀錄日期（含時分） | ✅ | MM/DD HH:MM 格式 |
| 14 | 工時明細顯示工時區間（選填） | ✅ | `startTimeText ~ endTimeText` |
| 15 | 工時明細顯示備註（選填） | ✅ | `log.note` |
| 16 | 長按單筆工時紀錄可刪除 | ✅ | 長按 500ms → 確認/取消 overlay |
| 17 | 編輯任務名稱 | ✅ | 在編輯彈窗中修改 |
| 18 | 任務卡片顯示紀錄筆數 | ✅ | 右側「N筆」小標籤 |

---

## 3. 工時紀錄

| # | 功能 | 狀態 | 備註 |
|---|------|------|------|
| 19 | 手動補登工時（小時/分鐘分開輸入） | ✅ | `LogTimeModal` |
| 20 | 輸入開始 / 結束時間（選填） | ✅ | `time` input |
| 21 | 輸入備註（選填） | ✅ | `textarea` |
| 22 | 儲存按鈕為橫向長條式 | ✅ | 「儲存任務與紀錄」 |
| 23 | 已投資工時與紀錄即時同步 | ✅ | `totalMinutes` via Firestore `increment()` |
| 24 | 刪除工時紀錄時同步遞減總時數 | ✅ | `increment(-duration)` |

---

## 4. 收款管理

| # | 功能 | 狀態 | 備註 |
|---|------|------|------|
| 25 | 新增收款紀錄（金額輸入） | ✅ | 可快速新增或展開詳填 |
| 26 | 手動輸入收款日期（選填） | ✅ | `date` input |
| 27 | 手動輸入收款備註（選填） | ✅ | `text` input |
| 28 | 收款紀錄列表（每一筆獨立顯示） | ✅ | 金額 + 日期 + 備註 |
| 29 | 刪除單筆收款紀錄 | ✅ | `close` icon |
| 30 | 收款新增組件動態展開（動畫） | ✅ | 點擊金額框展開完整表單 |
| 31 | 收款狀態標籤 | ✅ | 全額已收 / 部分收款 / 未收款 |

---

## 5. 專案數據監控

| # | 功能 | 狀態 | 備註 |
|---|------|------|------|
| 32 | 專案預算顯示 | ✅ | `formatCurrency` TWD |
| 33 | 目前實際時薪 | ✅ | `actualRate = budget / hours` |
| 34 | 已投資工時（與 logs 同步） | ✅ | 由 `totalMinutes` 加總計算 |
| 35 | 剩餘建議工時 | ✅ | `expectedHours - loggedHours` |
| 36 | 工時佔比進度條 | ✅ | 百分比動畫條 |
| 37 | 超時警示（紅色標記） | ✅ | `isOvertime` 判定 |

---

## 6. 設定頁面

| # | 功能 | 狀態 | 備註 |
|---|------|------|------|
| 38 | 全域目標時薪設定 | ✅ | `NumberAdjuster` 以 100 為單位 |
| 39 | 登出按鈕（圖示化） | ✅ | 右上角 `logout` icon |

---

## 7. UI / UX 規範

| # | 功能 | 狀態 | 備註 |
|---|------|------|------|
| 40 | 自定義 Toast 通知系統 | ✅ | 替代原生 `alert()` |
| 41 | 自定義 +/- 數字調節器 | ✅ | `NumberAdjuster` 組件 |
| 42 | 禁止 Emoji，使用 Material Symbols | ✅ | 全站統一 |
| 43 | Wabi-sabi 色彩規範（Earth Tones） | ✅ | sage / terracotta / wood / stone |
| 44 | 橫向儲存按鈕統一 | ✅ | 所有 Modal 一致 |
| 45 | 計時器功能已移除 | ✅ | 純手動輸入取代 |
| 46 | 底部導覽列（首頁/專案/設定） | ✅ | `BottomNav` 組件 |
